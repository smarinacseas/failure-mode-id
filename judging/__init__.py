"""E08 judge-panel policy: the plan §0.3 consensus rules (Opus tie-break,
EXCLUDE for undecidable criteria, completeness gate) as an isolated,
unit-tested module wired into the existing pipeline's aggregate stage."""
