# T01 environment — validated working combination as of 2026-07-16
# Install order matters:
#   1. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#      (adjust cu128 to match `nvidia-smi` CUDA version on THIS host — it varies by pod/driver)
#   2. pip install -r requirements-t01.txt  (this file, torch lines excluded on purpose)
# Do NOT `pip freeze > this file` wholesale — it pulls in unrelated system packages
# (dbus-python, tilelang, supervisor, etc.) that break installs. Keep this file hand-curated.
accelerate==1.14.0
bitsandbytes==0.49.2
datasets==5.0.0
numpy==2.1.2
pandas==3.0.3
peft==0.19.1
scipy==1.18.0
statsmodels==0.14.6
torch==2.8.0+cu128
torchaudio==2.8.0+cu128
torchvision==0.23.0+cu128
transformers==5.14.1
trl==1.8.0
